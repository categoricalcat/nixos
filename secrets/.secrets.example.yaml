passwords:
    yi: string
    workd: string
    nextcloud: string
tokens:
    github-runner-nixos: string
    playit-agent: |
        SECRET_KEY=string
    wg-easy: |
        PASSWORD_HASH=string
    cloudflare-ddclient: string
    cloudflare-acme: string
    cloudflared: string
    localtonet-authtoken: string
    deepseek: string
ngrok:
    config: |-
        agent:
          authtoken: string
        version: "3"
samba:
    credentials:
        yi: |
            username=string
            password=string
services:
    htpasswd: |
        admin:$2y$05$bcrypthashhere
zerotier:
    network_id: string
