passwords:
    yi: string
    workd: string
    nextcloud: string
tokens:
    forgejo-runner: TOKEN=string
    github-runner-nixos: string
    attic-server-jwt-env: |
        ATTIC_SERVER_TOKEN_RS256_SECRET_BASE64=<base64-rsa-key>
    attic-push-token: string
    playit-agent: |
        SECRET_KEY=string
    cloudflare-acme: string
    cloudflared: string
    localtonet-authtoken: string
    deepseek: string

samba:
    credentials:
        yi: |
            username=string
            password=string
services:
    htpasswd: |
        admin:$2y$05$bcrypthashhere
    grafana:
        secret-key: string
netbird:
    setup_key: string
